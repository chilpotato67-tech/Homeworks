
`src/` — Vagrant конфіг для 2 VM (Jenkins master + Jenkins worker)  
`src/jenkins-vagrant/` — Vagrantfile + provisioning (підняття VM)  
`src/.vagrant/` — службова папка Vagrant (локальна)

`RepoStep/` — код Node.js застосунку + pipeline файли  
`RepoStep/index.js` — Node.js app  
`RepoStep/package.json` — залежності + scripts  
`RepoStep/tests/` — тести  
`RepoStep/Dockerfile` — Docker image build  
`RepoStep/Jenkinsfile` — Jenkins pipeline

`screen/` — скріншоти виконання (докази роботи)
