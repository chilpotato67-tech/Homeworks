#!/bin/bash

mkdir -p ~/watch

while true; do
  for file in ~/watch/*; do
    if [[ "$file" == *.back ]]; then
      continue
    fi

    if [[ -f "$file" ]]; then
      cat "$file"
      mv "$file" "$file.back"
    fi
  done
  sleep 5
done
