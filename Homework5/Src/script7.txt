#!/bin/bash

# $1 — це перший аргумент, який передає користувач (ім’я файлу)
filename=$1

# рахуємо кількість рядків у файлі за допомогою wc -l
lines=$(wc -l < "$filename")

# виводимо результат
echo "Number of lines is: $lines"
