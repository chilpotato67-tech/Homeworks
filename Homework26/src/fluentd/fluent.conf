<source>
  @type forward
  port 24224
  bind 0.0.0.0
</source>

<match docker.**>
  @type opensearch
  host elasticsearch
  port 9200
  scheme http
  logstash_format true
  logstash_prefix docker-logs
  include_tag_key true
  tag_key @log_name
  flush_interval 5s
</match>
